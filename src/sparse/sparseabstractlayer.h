#ifndef SPARSEABSTRACTLAYER_H
#define SPARSEABSTRACTLAYER_H

#include <abstractlayer.h>


/*
class SparseAbstractLayer  : public AbstractLayer {
public:
    SparseAbstractLayer();

    SparseAbstractLayer(AbstractLayer * prev);

    SparseAbstractLayer(std::istream & stream, AbstractLayer * prev);

    virtual ~SparseAbstractLayer();


    virtual std::vector<number> &getSparseInput(size_t index) = 0;

    virtual std::vector<number> &getSparseOutput(size_t index) = 0;

    virtual std::vector<number> &getSparseRightErrorSignal(size_t index) = 0;

    virtual std::vector<number> &getSparseLeftErrorSignal(size_t index) = 0;

    virtual std::vector<size_t> &getErrorSignalActive(size_t index) = 0;

    virtual std::vector<size_t> &getOutputActive(size_t index) = 0;
};*/

#endif // SPARSEABSTRACTLAYER_H
